model Profile {
  id           String  @id @default(uuid())
  profilePhoto String?
  bio          String?
  userID       String  @unique
  user         User    @relation(fields: [userID], references: [id])
}
