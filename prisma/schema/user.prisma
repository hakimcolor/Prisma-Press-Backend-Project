model User {
  id           String   @id @default(ulid())
  name         String   @db.VarChar(255)
  email        String   @unique
  password     String
  activeStatus ActiveSt @default(ACTIVE)
  role         Role     @default(USER)
}

